<nav class="navbar navbar-default navbar-static-top bg-blue">
  <div class="container">
    <div class="navbar-header">
      <buttom type="buttom" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#menu" aria-expanded="false">
        <span class="sr-only">Toggle Navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </buttom>
      <%= link_to "Lancer", root_path, class: "navbar-brand"%>
    </div> <!-- end navbar-header -->
    <div class="collapse navbar-collapse" id="menu">
      <div class="navbar-left">
        <a href="#" class="btn btn-default navbar-btn">Browse all Jobs</a>
        <%= link_to new_gig_path, class: "btn btn-warning navbar-btn" do %>
          Post a Job
          <span class="glyphicon glyphicon-menu-right small"></span>
        <% end %>
      </div>
      <div class="navbar-right login">
        <% if user_signed_in? %>
          <%= link_to "Edit Profile", edit_user_registration_path, class: "btn btn-info navbar-btn" %>
          <%= link_to "Sign Out", destroy_user_session_path, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-default navbar-btn"%>
        <% else %>
          <%= link_to "Sign In", new_user_session_path, class: "btn btn-default navbar-btn" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-warning navbar-btn"%>
        <% end %>
      </div>
    </div> <!-- end menu div -->
  </div> <!-- end container div -->
</nav>
